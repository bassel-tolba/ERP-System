# File: gipcco_project/inventory/models.py
- **Purpose:** Serves as the central import point for all models, which have been refactored into smaller files inside the 'models' directory. This maintains backward compatibility with the rest of the Django project, as imports like `from inventory.models import Product` will continue to work.
