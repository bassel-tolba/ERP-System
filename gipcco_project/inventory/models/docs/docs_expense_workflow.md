# File: gipcco_project/inventory/models/expense_workflow.py
- **Purpose:** Defines models for managing the expense request and approval workflow.

- `ExpenseLog`: Represents a single, general expense record.
- `ExpenseRequest`: Represents a request for an expense, which goes through an approval process.
